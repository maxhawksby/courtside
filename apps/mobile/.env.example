# Copy to .env (gitignored). Values from the Supabase project dashboard
# (Settings → API) or `npx supabase status` when running locally.
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=replace-with-anon-key
