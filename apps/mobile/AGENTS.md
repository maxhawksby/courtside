# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Running on the device

Always start the dev loop with `npm run dev` (wraps `../../scripts/dev-doctor.sh` -
preflights networking/Docker/Supabase, then `expo start` in LAN mode). Never `--tunnel`.
